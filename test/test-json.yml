- hosts: localhost
  tasks:
    - name: test json module
      json_module:
        path: "~/playbooks/data/test.json"
        changes:
          - type: "set"
            path: "shape"
            value: "triangular"
          - type: "set"
            path: colors.[1].color
            value: "orange"
          - type: "set"
            path: "price"
            value: 100
