- hosts: localhost
  tasks:
    - name: test json module
      json_module:
        path: "data/test.json"
        changes:
          - type: "set"
            path: "shape"
            value: "triangular"
