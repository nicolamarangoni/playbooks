- hosts: '{{ host_boot }}'
  tasks:
  - name: Stop other nodes
    systemd:
      name: mariadb
      daemon_reload: yes
      state: stopped
- hosts: '{{ host_join }}'
  tasks:
  - name: Stop first node
    systemd:
      name: mariadb
      daemon_reload: yes
      state: stopped
